# ── Google Gemini ──────────────────────────────────────────────
GOOGLE_API_KEY=your_google_api_key_here
GEMINI_MODEL=gemini-2.5-flash
GEMINI_TEMPERATURE=0.0

# ── PostgreSQL (Docker Compose defaults) ───────────────────────
DATABASE_URL=postgresql+asyncpg://hustbot:hustbot@localhost:5432/hustbot_db

# ── RAG Pipeline ───────────────────────────────────────────────
# Đường dẫn tương đối từ thư mục backend/
CHROMA_DIR=../chroma_db
DATA_DIR=../data

RETRIEVER_K=6
DEVICE=cpu

# ── Memory ─────────────────────────────────────────────────────
SLIDING_WINDOW_SIZE=8
