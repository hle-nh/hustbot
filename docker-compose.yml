services:
  postgres:
    image: postgres:16-alpine
    container_name: hustbot_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: hustbot
      POSTGRES_PASSWORD: hustbot
      POSTGRES_DB: hustbot_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hustbot -d hustbot_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: hustbot_backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      # Volume Mapping để hot-reload khi dev, comment out khi production thực sự nếu không muốn mount code.
      - ./backend:/app
      # Lưu data và db ra ngoài container
      - ./chroma_db:/app/chroma_db
      - ./data:/app/data
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - ./backend/.env
    # Chạy lệnh reload để code thay đổi thì server cập nhật ngay
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: hustbot_frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  pgdata:
    name: hustbot_pgdata
